---
title: 计组第四章：指令系统
date: 2026-04-28 13:56:04
categories:
  - note
  - 408
  - 计算机组成原理
tags: [计组]
index_img:
banner_img: /images/壁纸.jpg
---

## 指令系统

### 指令集体系结构

&nbsp;&nbsp;&nbsp;&nbsp;机器指令是指示计算机执行某种操作的命令。是计算机运行的最小功能单位。一台计算机的所有指令的集合构成该机的指令系统（指令集）。指令系统是指令集体系结构(ISA)中最核心的部分。

&nbsp;&nbsp;&nbsp;&nbsp;ISA规定的内容主要包括：

- 指令格式，指令寻址方式，操作类型，以及每种操作对应的操作数的相应规定。
- 操作数的类型，操作数寻址方式，以及是按大端方式还是按小端方式存放。
- 程序可访问的寄存器编号、个数和位数，存储空间的大小和编址方式。
- 指令执行过程的控制方式等，包括程序计数器、条件码定义等。

### 指令的基本结构

&nbsp;&nbsp;&nbsp;&nbsp;一条指令通常包括操作码字段和地址码字段两部分

| 操作码字段                 | 地址码字段                         |
| -------------------------- | ---------------------------------- |
| 执行什么操作，具有何种功能 | 给出被操作的信息(指令或数据)的地址 |


#### 按操作码数目分
根据指令中操作数地址码数目的不同可分为：

1. 零地址指令：只给出操作码OP，没有显式地址
    ![零地址指令](../images/指令系统/零地址指令.png)
2. 一地址指令
    ![一地址指令](../images/指令系统/一地址指令.png)
3. 二地址指令
    ![二地址指令](../images/指令系统/二地址指令.png)
4. 三地址指令
    ![三地址指令](../images/指令系统/三地址指令.png)
5. 四地址指令
    ![四地址指令](../images/指令系统/四地址指令.png)

地址位数与寻址关系：
&nbsp;&nbsp;&nbsp;&nbsp;若指令字长为 32位，操作码占8位，1个地址码字段占 24位，则指令操作数的直接寻址范围为 $2^{24}$=16M。若地址码字段均为主存地址，则完成一条一地址指令需要3次访存(取指令1次，取操作数1次，存结果1次)。

#### 按指令字长分

&nbsp;&nbsp;&nbsp;&nbsp;指令字长：一条指令所包含的二进制代码的位数，取决于操作码、地址码的长度和地址码的个数。

> 与机器字长没有固定关系。
> 单字长指令：指令长度等于机器字长，只需访存一次就能将指令完整取出
> 半字长指令：指令长度等于半个机器字长
> 双字长指令：指令长度等于两个机器字长，需要访存两次才能完整取出，耗费两个存取周期

&nbsp;&nbsp;&nbsp;&nbsp;定长指令字结构：所有指令的长度都是相等的，执行速度快，控制简单

&nbsp;&nbsp;&nbsp;&nbsp;变长指令字结构：各种指令的长度随指令功能而异

> 由于主存一般是按字节编址的，因此指令字长通常为字节的整数倍

#### 按操作码长度分

定长操作码：指令系统中所有指令的操作码长度都相同

> 一般n位操作码字段的指令系统最大能够表示 $2^n$条指令。定长操作码对于简化计算机硬件设计，提高指令译码和识别速度很有利。当计算机字长为32位或更长时，这是常规用法。

可变长操作码：指令系统中各指令的操作码长度可变

#### 按操作类型分

1. 数据传送：传送指令通常有寄存器之间的传送(MOV)、从内存单元读取数据到CPU寄存器从 CPU 寄存器写数据到内存单元(STORE)、进栈操作(PUSH)、出栈操作(POP)，存储器中的数据放到寄存器中（LOAD）等。

2. 算术和逻辑运算：
3. 移位操作：算术移位、逻辑移位、循环移位等
4. 转移操作：无条件转移(JMP)、条件转移(BRANCH)、调用(CALL)、返回(RET)、陷阱(TRAP)等。
5. 输入输出操作：CPU寄存器与IO端口（IO接口中的寄存器）之间的数据传送

### 扩展操作码指令格式

定长指令字结构+可变长操作码，不同地址数的指令使用不同长度的操作码

![扩展操作码1](../images/计组第四章/扩展操作码1.png)

- 不能允许短码是长码的前缀，即短操作码不能与长操作码的前面部分的代码相同
- 各指令的操作码一定不能重复

> 通常情况下，对使用频率较高的指令，分配较短的操作码，对使用频率较低的指令，分配较长的操作码，从而尽可能减少指令译码和分析的时间

![扩展操作码2](../images/计组第四章/扩展操作码2.png)

## 指令的寻址方式

&nbsp;&nbsp;&nbsp;&nbsp;寻找下一条将要执行的指令地址称为指令寻址；寻找本条指令的数据地址称为数据寻址

### 指令寻址

- 顺序寻址：通过程序计数器PC加1（一条指令长度），自动形成下一条指令的地址。

	> PC自增的大小与编址方式、指令字长有关，现代计算机通常是按字节编址的，若指令字长为 16 位，则 PC自增为(PC)+2（增加两个字节）;若指令字长为 32 位，则PC自增为(PC)+4（增加四个字节）

- 跳跃寻址：通过转移类指令实现。跳跃是指由本条指令给出下条指令地址的计算方式，而是否跳跃可能受到状态寄存器的控制。

    > 跳跃的方式分为绝对转移(地址码直接指出转移目标地址)和相对转移(地址码指出转移目的地址相对于当前 PC值的偏移量)，由于 CPU 总是根据 PC的内容去主存取指令的，因此转移指令执行的结果是修改PC值，下一条指令仍然通过 PC给出。

### 数据寻址

&nbsp;&nbsp;&nbsp;&nbsp;如何在指令中表示一个操作数的地址，或怎样计算出操作数的地址。为区别各种方式，通常在指令字中设置一个寻址特征字段，用来指明属于哪种寻址方式(其位数决定了寻址方式的种类)

| 操作码 | 寻址特征 | 形式地址A |
| ------ | -------- | --------- |

&nbsp;&nbsp;&nbsp;&nbsp;指令中的地址码字段并不代表操作数的真实地址，这种地址称为形式地址(A)。形式地址结合寻址方式，可以计算出操作数在存储器中的真实地址，这种地址称为有效地址(EA)。

> A表示A这个地址,(A)表示地址为A里面的内容
>
> EA=A表示形式地址A就是真实地址EA
>
> EA=(A)表示形式地址A的内容是真实地址EA

- 若为立即寻址，则形式地址的位数决定了操作数的范围。
- 若为直接寻址，则形式地址的位数决定了可寻址的范围。
- 若为寄存器寻址，则形式地址的位数决定了通用寄存器的最大数量。
- 若为寄存器间接寻址，则寄存器的位数决定了可寻址的范围。

#### 直接寻址

指令字中的形式地址A就是操作数的真实地址，即$EA=A$

![直接寻址](../images/计组第四章/直接寻址.png)

#### 间接寻址

![间接寻址](../images/计组第四章/间接寻址.png)

#### 寄存器寻址

![寄存器寻址](../images/计组第四章/寄存器寻址.png)

#### 寄存器间接寻址

![寄存器间接寻址](../images/计组第四章/寄存器间接寻址.png)

#### 隐含寻址

![隐含寻址](../images/计组第四章/隐含寻址.png)

#### 立即寻址

![立即寻址](../images/计组第四章/立即寻址.png)

#### 基址寻址

程序员自己决定哪里作为偏移的起点

![基址寻址](../images/计组第四章/基址寻址.png)

![基址寻址的作用](../images/计组第四章/基址寻址的作用.png)

> 机制寄存器是面向操作系统的，其内容由操作系统或管理程序确定。在程序执行过程中，基址寄存器的内容不变，形式地址可变
>
> 当采用通用寄存器作为基址寄存器时，可由用户决定哪个寄存器作为基址寄存器，但其内容仍由操作系统确定

#### 变址寻址

![变址寻址](../images/计组第四章/变址寻址.png)

![变址寻址的作用](../images/计组第四章/变址寻址的作用.png)

用于访问数组，同时也可以当循环中的局部变量

**基址变址复合寻址**

![基址变址复合寻址](../images/计组第四章/基址变址复合寻址.png)

#### 相对寻址

以下一条指令的地址为偏移的起点

![相对寻址](../images/计组第四章/相对寻址.png)

![相对寻址的作用](../images/计组第四章/相对寻址的作用.png)

子函数内部采用相对寻址，这样整个子函数移动时，内部采用相对寻址仍能够正确找到对应的下条指令

#### 堆栈寻址

硬堆栈：

![堆栈寻址1](../images/计组第四章/堆栈寻址1.png)

![堆栈寻址2](../images/计组第四章/堆栈寻址2.png)

硬堆栈不访存，软堆栈访存一次

## 程序的机器级代码表示（主要介绍x86汇编指令）

机器级代码：汇编语言+二进制代码（机器语言）

> 要能够看懂汇编语言，汇编语言与机器语言能够一一对应，要能结合汇编语言分析机器语言的指令格式、寻址方式

### x86汇编语言指令基础

指令的作用：改变程序的执行流 or 处理数据

#### 实现数据转移

![mov指令1](../images/计组第四章/mov指令1.png)

mov指令不支持两个操作数都来自于主存，必须有一个来自于寄存器或者常数

![mov指令2](../images/计组第四章/mov指令2.png)

![x86架构寄存器1](../images/计组第四章/x86架构寄存器1.png)

![x86架构寄存器2](../images/计组第四章/x86架构寄存器2.png)

#### 算术运算

![常见算数运算指令](../images/计组第四章/常见算数运算指令.png)

d为最后将数据存储到的位置，因此d只能是主存或者寄存器，不允许两个操作数同时来自于主存

32位数的除法需要扩展为64位再进行除法运算

reg：寄存器（register）    men：内存（memory）    con：常数（constant）

#### 逻辑运算

![常见逻辑运算指令](../images/计组第四章/常见逻辑运算指令.png)

实现分支结构、循环结构的指令：cmp、test、jmp、jxx

实现函数调用的指令：push、pop、call、ret

### 汇编指令格式

分为AT&T格式和Intel格式，上文位Intel格式，为Windows的常用格式

![两种格式对比](../images/计组第四章/两种格式对比.png)

### 选择语句

程序计数器PC（Program Counter）通常被称为IP（Instruction Pointer）

![jmp指令](../images/计组第四章/jmp指令.png)

![条件转移指令](../images/计组第四章/条件转移指令.png)

![选择语句机器级表示例子](../images/计组第四章/选择语句机器级表示例子.png)

汇编语言中，一般回以函数名作为标号，标注该函数指令的起始地址

![cmp指令底层原理](../images/计组第四章/cmp指令底层原理.png)

#### 循环语句机器级表示

![条件转移指令实现循环](../images/计组第四章/条件转移指令实现循环.png)

![loop指令实现循环](../images/计组第四章/loop指令实现循环.png)

只能用ecx作为循环计数器

### 函数调用的机器级表示

#### call ret指令

![call ret指令](../images/计组第四章/call ret指令.png)

#### 访问栈帧

![栈指针寄存器](../images/计组第四章/栈指针寄存器.png)

![push pop指令](../images/计组第四章/push pop指令.png)

![mov访问栈帧数据](../images/计组第四章/mov访问栈帧数据.png)

#### 切换栈帧

call指令作用：

1. 将IP旧值压栈保存（效果相当于push IP）
2. 设置IP新值，无条件转移至被调用函数的第一条指令

![记录上层函数的栈帧基址](../images/计组第四章/记录上层函数的栈帧基址.png)

![leave指令](../images/计组第四章/leave指令.png)

ret指令：从函数的栈帧顶部找到IP旧值，将其出栈并恢复IP寄存器

#### 传递参数和返回值

![栈帧包含的内容](../images/计组第四章/栈帧包含的内容.png)

![数据传递示例](../images/计组第四章/数据传递示例.png)

在函数调用前如有一些数据也是存在这些寄存器里的，可以先将数据压栈保存，调用结束后再从栈内弹出即可

## 指令设计方向

![指令设计方向](../images/计组第四章/指令设计方向.png)

![指令设计方向对比](../images/计组第四章/指令设计方向对比.png)
