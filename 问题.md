语句覆盖：使程序中的每个语句至少都能被执行一次

判定覆盖：使程序中每个判断的真值和假值都出现一次

> 只要满足判定覆盖，就必定会满足语句覆盖

条件覆盖：判断表达式中的每一个条件都要至少取得一次真值和假值

> 不追求整体的真假，例如1&&0和0&&1就能满足条件覆盖，但不满足判定覆盖

条件/判定覆盖：同时满足判定覆盖和条件覆盖

条件组合覆盖：使每个判定中条件取值的各种组合都至少出现一次

